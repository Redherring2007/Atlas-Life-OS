[phases.setup]
nixPkgs = ["python311", "ffmpeg"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "sh -c 'python bot.py & python -m uvicorn mini_app:app --host 0.0.0.0 --port 8000'"
